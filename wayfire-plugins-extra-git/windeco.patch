Submodule subprojects/windecor contains modified content
diff --git a/subprojects/windecor/deco-subsurface.cpp b/subprojects/windecor/deco-subsurface.cpp
index 09a6b61..7d13ad5 100644
--- a/subprojects/windecor/deco-subsurface.cpp
+++ b/subprojects/windecor/deco-subsurface.cpp
@@ -23,13 +23,13 @@ class simple_decoration_surface : public wf::surface_interface_t, public wf::com
     bool _mapped = true;
 
     wayfire_view view;
-    wf::signal_callback_t title_set = [ = ] ( wf::signal_data_t *data ) {
+    wf::signal_connection_t title_set = [ = ] ( wf::signal_data_t *data ) {
                                           if ( get_signaled_view( data ) == view ) {
                                               view->damage(); // trigger re-render
                                           }
                                       };
 
-    wf::signal_callback_t sticky_set = [ = ] ( wf::signal_data_t *data ) {
+    wf::signal_connection_t sticky_set = [ = ] ( wf::signal_data_t *data ) {
                                            if ( get_signaled_view( data ) == view ) {
                                                theme.is_sticky = view->sticky;
                                                for ( auto& renderable: layout.get_renderable_areas() ) {
@@ -86,8 +86,8 @@ class simple_decoration_surface : public wf::surface_interface_t, public wf::com
         }
 
         virtual ~simple_decoration_surface() {
-            view->disconnect_signal( "title-changed", &title_set );
-            view->disconnect_signal( "set-sticky",    &sticky_set );
+            view->disconnect_signal( &title_set );
+            view->disconnect_signal( &sticky_set );
         }
 
         /* wf::surface_interface_t implementation */
